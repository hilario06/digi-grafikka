<div data-search-designs-target="list" style='margin-bottom: 150px;'>
  <p class=" text-muted">
    <% if params[:query].blank? %>
      <%= pluralize designs.count, 'diseños' %> disponibles
    <% else %>
      <%= pluralize designs.count, 'resultados' %> de <code><%= params[:query] %></code>
    <% end %>
  </p>
  <div class="auto-grid-designs"
    data-insert-design-in-list-target="items"
    data-controller="delete-design">
    <% designs.each do |design| %>
      <%= render 'design', design: design %>
    <% end %>
  </div>
</div>
